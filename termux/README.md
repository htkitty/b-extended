### Clarity
Besides the prepackaged binaries, this is where this fork shines.

Most android phones that can run Termux are indeed aarch64, and this fork contains:
1. how to build on your phone
2. prebuilt aarch64 binary
